#!/usr/bin/env node

var geobuf = require('../'),
    rw = require('rw');

var byteBuffer = geobuf.featureToGeobuf(JSON.parse(rw.readFileSync('/dev/stdin')));
process.stdout.write(byteBuffer.toBuffer());
